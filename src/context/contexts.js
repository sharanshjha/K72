import { createContext } from 'react'

// Export contexts from a dedicated file to avoid Fast Refresh warnings
export const NavbarContext = createContext()
export const NavbarColorContext = createContext()
